baseURL = 'https://artificialinsights.tech/' 
languageCode = 'en-us'
title = 'Artificial Insights'
theme = 'mainroad'
pagination.pagerSize = "5" # Number of posts per page

[Params]
  description = "Exploring AI: Innovations, Insights, and the Future of Tech" # Logo subtitle" # Description of your site
  opengraph = true
  twitter_cards = true
  readmore = false # Show "Read more" button in list if true
  authorbox = false
  pager = true
  post_meta = ["date", "categories"] # Order of post meta information
  mainSections = ["post"]

[Params.logo]
  subtitle = "Exploring AI: Innovations, Insights, and the Future of Tech" # Logo subtitle
  image = "/img/artificial-insights.png"

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["social", "recent", "taglist"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)
  social.twitter = "HelloArtificial"
  social.youtube = "@HelloArtificial" 
  social.spotify = "artificial-insights-pod"

[markup.goldmark.renderer]
unsafe = true